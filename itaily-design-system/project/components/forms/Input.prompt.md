Single-line text input — terracotta focus ring, optional leading icon, label, hint/error.

```jsx
<Input label="Email" icon="mail" placeholder="you@studio.it" />
<Input label="Case number" error="Required field" />
```

Sizes `sm | md | lg`. Pass `error` to show the red state; `hint` for helper text.
