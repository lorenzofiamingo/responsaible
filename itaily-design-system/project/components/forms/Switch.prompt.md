On/off toggle. Terracotta track when on.

```jsx
<Switch label="Cite primary sources only" checked={x} onChange={setX} />
```

Controlled via `checked` + `onChange(next)`. Omit `label` for a bare switch.
