Inline legal reference chip — the signature of an Itaily answer. Monospace, terracotta.

```jsx
Liability follows from <Citation index={1} onClick={open}>Art. 2043 c.c.</Citation>
```

Pass `index` for a numbered footnote chip. `active` highlights it (terracotta fill) when its source panel is open.
