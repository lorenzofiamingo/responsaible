Three-segment confidence indicator for AI answers — Itaily never shows blind certainty.

```jsx
<ConfidenceMeter level="high" />
<ConfidenceMeter level="low" showLabel={false} />
```

`level`: low (red) / medium (amber) / high (green).
