Square, icon-only button for toolbars and compact actions.

```jsx
<IconButton icon="copy" label="Copy answer" />
<IconButton icon="send" label="Send" variant="solid" />
```

Variants: `ghost` (default), `solid` (terracotta), `outline`. Sizes `sm | md | lg`. Always pass `label` for accessibility — it doubles as the tooltip.
